{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "set ELECTRON_START_URL=http://localhost:3000&& yarn run build && electron ./dist/main.js",
    "build": "tsc",
    "start": "electron ./dist/main.js"
  },
  "devDependencies": {
    "electron": "^12.0.5"
  },
  "dependencies": {
    "ip-regex": "^4.3.0",
    "tlds": "^1.221.0"
  }
}
