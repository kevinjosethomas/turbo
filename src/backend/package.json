{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "set ELECTRON_START_URL=http://localhost:3000&& electron .",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^12.0.5"
  }
}
